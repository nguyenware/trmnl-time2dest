{
  "name": "trmnl-drive-time",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "TRMNL plugin backend: live drive time, fastest route, incidents and an e-ink route map.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env scripts/dev.js",
    "test": "node --test"
  }
}
